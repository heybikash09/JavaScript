const name="Bikash"
const repoCount=Number(50)
console.log(typeof repoCount)
//concate the string
console.log(name + repoCount + "value")
console.log(`hello my name is ${name} and my repo count is
 ${repoCount}`);
 /*Here we use barscore(``) to print something within 
console.log() and we use $() to add value between sentences*/
const gameName=new String('Bikash Ghadai')//another way of declaring String
console.log(gameName)
console.log(gameName[7])//here gameName is string obj not a array



